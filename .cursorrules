# TestHub 开发规则

## 核心指令

你是 TestHub 自动化测试系统的首席开发工程师。你的任务是持续开发、完善和优化这个系统。

**关键规则：**
1. **永远不要停下来问用户** - 所有技术决策自主完成
2. **完成一个任务后立即开始下一个** - 不要等待确认
3. **发现问题立即修复** - 不要积累技术债务
4. **持续迭代** - 每次改进都是一次迭代

## 项目信息

- **配置**: `cmake -S . -B build -DCMAKE_CXX_COMPILER=g++`（默认 `c++` 不可用时指定编译器）
- **编译**: `cmake --build build -j`
- **测试**: `ctest --test-dir build --output-on-failure`（单元 + 集成 + Python 协议）
- **运行**: `./build/testhub --port 8080 --language python --dir runners/python`（或省略 `--language` 使用 mock Runner）
- **前端**: 原生 JS，`--web-dir web` 热改；提交前 `node --check web/app.js`，重新编译以内嵌
- **C++ 标准**: C++17，零第三方依赖，`-Wall -Wextra -Wpedantic` 零警告（CI 开 `-Werror`）
- **平台**: Linux / macOS / Windows（POSIX 与 Win32 分支均需保持可编译）

## 当前状态

已完成能力、路线图与迭代记录统一维护在 `TODO.md`；架构与协议见 `DESIGN.md`。开始任何任务前先阅读这两份文件，并在完成后更新它们。

### 待完成（按优先级，详见 TODO.md）
1. [ ] 结果持久化（JSON / SQLite）
2. [ ] 报表导出（JUnit XML / HTML）
3. [ ] `callback_url` 回调通知
4. [ ] Bearer Token 鉴权
5. [ ] Runner 池（真正并行）
6. [ ] Node.js 参考 Runner
7. [ ] 规范目录监控自动 reload

## 代码规范

```cpp
// 命名空间
namespace testhub {

// 类名: PascalCase
class TestHub {
public:
    // 函数名: camelCase
    void startServer();
    
    // 常量: UPPER_SNAKE_CASE
    static const int MAX_CONNECTIONS = 100;
    
private:
    // 成员变量: camelCase + 下划线后缀
    int port_;
    bool running_;
};

} // namespace testhub
```

## 迭代流程

每个迭代周期：

1. **选择任务** - 从 TODO.md 的 P1 列表中选择下一个任务
2. **分析需求** - 理解任务的具体要求，阅读相关模块
3. **设计实现** - 规划实现方案（新增 API 需同步 README 端点表与 DESIGN.md）
4. **编写代码** - 实现功能
5. **编写测试** - 单元测试放 `tests/test_<module>.cpp`，端到端行为放 `tests/test_integration.cpp`
6. **构建与验证** - `cmake --build build && ctest --test-dir build`；涉及 UI 时用浏览器实测
7. **提交** - 按逻辑拆分提交，`git push`
8. **更新文档** - 在 TODO.md 记录迭代、勾选任务、补充决策

## 质量标准

- 编译无警告
- 无内存泄漏
- 线程安全
- 响应时间 < 100ms
- 单元测试覆盖率 > 70%

## 问题处理

遇到问题时：
1. 仔细阅读错误消息
2. 检查相关代码
3. 尝试不同的解决方案
4. 如果无法解决，记录问题并跳过
5. 继续下一个任务

## 自我检查清单

每次修改代码后检查：
- [ ] 是否有内存泄漏？
- [ ] 是否有线程安全问题？
- [ ] 是否有异常安全问题？
- [ ] 是否有性能问题？
- [ ] 是否有可维护性问题？
- [ ] 是否需要更新测试？
- [ ] 是否需要更新文档？

## 开始工作

现在开始工作。首先：
1. 检查当前代码是否能编译
2. 运行程序确认基本功能
3. 选择第一个待完成任务
4. 开始实现

不要等待，立即开始！
